# Tâche terminée : CLI-002 — Commande `arc help` + branding ASCII

## Statut final
✅ Terminée — 2026-05-02
Durée réelle : ~40 min
PR : https://github.com/johannKionghat/EUGLOWLABS-ARC/pull/4 (mergée, squash, commit `81e34aa`)

## Bilan
- **Ce qui s'est bien passé** : plan en 5 sous-tâches respecté à 100%, aucune surprise, 7/7 tests verts du premier coup, CI verte premier essai (Node 28s + Go 26s).
- **Ce qui a posé problème** : rien. Tâche la plus fluide jusqu'ici.
- **Ce qu'on a appris** : `cli.usage(null)` (clipanion) retourne directement la string de l'aide globale → injection du banner triviale. Pattern de "remplacement de Builtins" propre, à reprendre si on doit injecter du comportement custom dans d'autres built-ins.
- **Décisions prises** : aucune ADR-level. Conformes aux ADRs existants.
  - Banner ASCII pur (pas d'unicode, pas d'ANSI maison) pour rendu cross-terminal
  - `Builtins.HelpCommand` remplacé par notre `HelpCommand` (3 paths : `help`, `-h`, `--help`)
  - L'aide d'une sous-commande reste sans banner (rendu interne clipanion)

---

## Objectif (rappel)
Donner au CLI son identité visuelle dès l'aide racine. Ajouter une commande `arc help` qui affiche un header ASCII "EuglowLabs ARC" suivi de l'aide générée par clipanion, sur les trois points d'entrée racine (`help`, `-h`, `--help`).

## Critères d'acceptation — tous validés
- [x] `arc help` affiche le banner ASCII puis la liste des commandes
- [x] `arc -h` et `arc --help` affichent le même banner + aide
- [x] `arc version --help` (aide d'une sous-commande) n'affiche **pas** le banner
- [x] Banner purement ASCII (lisible cmd.exe, terminaux minimaux, CI loggers)
- [x] 7/7 tests Vitest verts (4 anciens + 3 nouveaux)
- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [x] CI verte (Node 28s + Go 26s)
- [x] PR mergée sur main (#4 → 81e34aa)

## Smoke test reproduit en local
```
$ node packages/arc-cli/dist/index.js help
  +----------------------------------------------------------+
  |                    EuglowLabs ARC                        |
  |               Autonomous Resource Cloud                  |
  +----------------------------------------------------------+

━━━ EuglowLabs ARC - 0.0.0 ━━━
  arc help     Show the EuglowLabs ARC help.
  arc version  Print the EuglowLabs ARC CLI version.
```

## Fichiers livrés
- `packages/arc-cli/src/banner.ts` (création — `BANNER` + `renderBanner()`)
- `packages/arc-cli/src/commands/help.ts` (création — `HelpCommand`)
- `packages/arc-cli/src/cli.ts` (modif — remplace `Builtins.HelpCommand`)
- `packages/arc-cli/src/cli.test.ts` (modif — 3 nouveaux cas)
- `tasks/completed/2026-05-02-CLI-001.md` (archive CLI-001 post-merge)

## Plan d'implémentation (rappel) — 5 sous-tâches toutes complétées
1. Banner ASCII ✅
2. HelpCommand clipanion ✅
3. Wiring dans cli.ts ✅
4. Tests (3 nouveaux) ✅
5. Vérif + commit + PR + merge ✅
