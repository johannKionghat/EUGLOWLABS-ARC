# Tâche terminée : CLI-008 — Génération template `docker-compose.agents.yml`

## Statut final
✅ Terminée — 2026-05-02
Durée réelle : ~40 min
PR : https://github.com/johannKionghat/EUGLOWLABS-ARC/pull/10 (mergée, squash, commit `bde84f0`)

## Bilan
- **Ce qui s'est bien passé** : 5 sous-tâches respectées, mêmes timings que CLI-007 (~40 min), copy-paste du pattern de la trilogie. Tests verts du premier coup. CI verte premier essai (Node 32s + Go 25s).
- **Ce qui a posé problème** : rien. La 3ème itération du même pattern est triviale.
- **Ce qu'on a appris** : la trilogie de templates est essentiellement du structurel + un test de durcissement par template. **Total trilogie : ~2h35** (CLI-006 1h15 + CLI-007 40 min + CLI-008 40 min). Le découpage par fichier `__templates__/*.eta` + `*-compose.ts` a payé sur 3 itérations consécutives — pattern à reprendre pour les futures phases (templates ARC Agent compose, templates Cloud, etc.).
- **Décisions prises** : aucune ADR-level. Conformes à ADR-0008.
  - `networks: external: true` (orchestration ordre par CLI-012)
  - `OPENCLAW_DEFAULT_MODEL=mistral:7b` valeur concrète alignée spec §8.1
  - API keys en `__REPLACE_ME__`
  - `deepagents` sur double-réseau = point de jonction documenté ADR-0008

---

## Objectif (rappel)
Dernier compose maison de la trilogie. À partir d'un `ArcConfig`, générer le compose qui ajoute la couche AI custom à `local-ai-packaged` : OpenClaw (gateway, port 3100) et DeepAgents (orchestration, port 3200), tous deux sur `ai_net` ; DeepAgents joint en plus `sandbox_net` pour exécuter le code généré (point de jonction documenté ADR-0008).

## Critères d'acceptation — tous validés
- [x] Template eta `docker-compose.agents.yml.eta` versionné
- [x] `ai_net` et `sandbox_net` déclarés `external: true`
- [x] `openclaw` : image GHCR, env complet, label Traefik, network ai_net
- [x] `deepagents` : env, label Traefik, networks ai_net + sandbox_net, depends_on openclaw
- [x] `.env.eta` étendu avec 4 nouvelles vars (OpenClaw + API keys)
- [x] `generateAgentsCompose(cfg): string` exporté
- [x] 4 tests Vitest
- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [x] CI verte (Node 32s + Go 25s)
- [x] PR mergée sur main (#10 → bde84f0)

## Fichiers livrés
- `packages/arc-cli/src/templates/__templates__/docker-compose.agents.yml.eta` (création)
- `packages/arc-cli/src/templates/__templates__/env.eta` (modif — 4 vars)
- `packages/arc-cli/src/templates/agents-compose.ts` (création)
- `packages/arc-cli/src/templates/agents-compose.test.ts` (création — 4 tests)
- `packages/arc-cli/src/templates/index.ts` (modif — export)
- `packages/arc-cli/src/templates/env.test.ts` (modif — 4 assertions)
- `tasks/completed/2026-05-02-CLI-007.md` (archive CLI-007)

## Plan d'implémentation (rappel) — 5 sous-tâches toutes complétées
1. Template `docker-compose.agents.yml.eta` ✅
2. Étendre `env.eta` + maj test env ✅
3. Générateur `generateAgentsCompose` + barrel ✅
4. 4 tests Vitest ✅
5. Vérif + commit + PR + merge ✅

## Statistiques projet à ce stade
- PRs mergées : **10** (zéro rollback)
- Tests Vitest : arc-cli (31) + arc-shared (8) = **39**
- Phase 1 : 8/28 (CLI-001 → 008 ✅)
- **Trilogie templates complète** : prod / sandbox / agents
