# INFRA-008 — Changesets ✅
- `@changesets/cli` devDep racine
- `.changeset/config.json` (access: public, baseBranch: main, ignore arc-cloud)
- scripts `changeset`, `version-packages`, `release` (pnpm build && changeset publish)
- arc-cloud (closed-source per ADR-0006) explicitement dans `ignore`
- Phase 0 : 8/10
