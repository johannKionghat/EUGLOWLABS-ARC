# INFRA-010 — Release workflow ✅
- `.github/workflows/release.yml`
  - PR: `pnpm changeset status` + `pnpm -r publish --dry-run`
  - push main: `changesets/action@v1` ouvre un version-PR ou publie via `pnpm release` (gated par NPM_TOKEN)
- Phase 0 : 10/10 ✅
