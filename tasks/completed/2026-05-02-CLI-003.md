# Tâche terminée : CLI-003 — Schéma zod de `arc.config.yml`

## Statut final
✅ Terminée — 2026-05-02
Durée réelle : ~1h20
PR : https://github.com/johannKionghat/EUGLOWLABS-ARC/pull/5 (mergée, squash, commit `8fa37de`)

## Bilan
- **Ce qui s'est bien passé** : plan en 7 sous-tâches respecté à 100%. Tests Vitest verts du premier coup (8/8). Découpe par section (un fichier = un sous-schéma) très lisible et testable. CI verte premier essai (Node 33s + Go 26s).
- **Ce qui a posé problème** : rien de bloquant. Biome a reformaté quelques `.some(...)` sur 3 lignes en une ligne (auto-fix), comportement attendu.
- **Ce qu'on a appris** : zod 4 (4.4.2) installé par défaut via `pnpm add zod` — API moderne avec `z.email()` et `z.strictObject()` au top-level. Les `superRefine()` retournent des paths typés correctement même en TS strict. Pattern par section (provider.ts, dns.ts, ...) à reprendre pour les schémas marketplace (MARKET-001) et pour le schéma de l'API ARC Cloud (CLOUD-002).
- **Décisions prises** : aucune ADR-level. Toutes locales et réversibles.
  - **snake_case** dans les schémas (boundary type matchant le YAML 1:1, simplifie CLI-004)
  - **`.strictObject()`** racine (typos remontent comme erreurs)
  - **2 invariants `superRefine`** : target=vps→provider obligatoire, subdomains uniques
  - **regex `domain` pragmatique** (pas RFC complète)
  - **zod 4** plutôt que 3 (latest, API plus propre)

---

## Objectif (rappel)
Définir le modèle de domaine central du fichier `arc.config.yml` (spec-infra §5.5) en zod, dans `@euglowlabs/arc-shared`. Source de vérité typée pour CLI / Agent / Cloud.

## Critères d'acceptation — tous validés
- [x] `arcConfigSchema` (zod) couvre tous les champs de spec-infra §5.5
- [x] Type TS `ArcConfig` exporté via `z.infer`
- [x] Sous-schémas séparés exportés : `providerSchema`, `dnsSchema`, `stackSchema`, `backupsSchema`, `servicesSchema`, `projectEntrySchema`
- [x] Invariants cross-field : `target=vps` → `provider` obligatoire ; `target=local` → optionnel
- [x] 8 tests Vitest (≥ 6 demandés) : minimal local, complet vps, defaults, email invalide, target hors enum, target=vps sans provider, subdomains dupliqués, clé inconnue (bonus strict)
- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [x] CI verte (Node 33s + Go 26s)
- [x] PR mergée sur main (#5 → 8fa37de)

## Fichiers livrés
- `packages/arc-shared/package.json` (modif — `zod 4.4.2`)
- `packages/arc-shared/src/schemas/provider.ts` (création)
- `packages/arc-shared/src/schemas/dns.ts` (création)
- `packages/arc-shared/src/schemas/stack.ts` (création)
- `packages/arc-shared/src/schemas/backups.ts` (création)
- `packages/arc-shared/src/schemas/services.ts` (création)
- `packages/arc-shared/src/schemas/project.ts` (création)
- `packages/arc-shared/src/schemas/config.ts` (création — schéma racine + `superRefine`)
- `packages/arc-shared/src/schemas/index.ts` (création — barrel)
- `packages/arc-shared/src/index.ts` (modif — re-export `./schemas`)
- `packages/arc-shared/src/schemas/config.test.ts` (création — 8 tests)
- `pnpm-lock.yaml` (modif)
- `tasks/completed/2026-05-02-CLI-002.md` (archive CLI-002)

## Plan d'implémentation (rappel) — 7 sous-tâches toutes complétées
1. Dépendance zod + bootstrap dossier ✅
2. Sous-schémas provider, dns, stack ✅
3. Sous-schémas backups, services, project ✅
4. Schéma racine + `superRefine` ✅
5. Barrel + re-export public ✅
6. Tests Vitest (8 cas) ✅
7. Vérif + commit + PR + merge ✅

## Statistiques projet à ce stade
- PRs mergées : 5
- Tests Vitest : arc-cli (7) + arc-shared (8) = **15**
- Phase 1 : 3/28 (CLI-001, CLI-002, CLI-003 ✅)
