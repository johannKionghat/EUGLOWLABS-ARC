# CLI-020 — `arc restore` ✅
PR #22 → `db4967f`
- `listBackups` (db_*.sql, volume_*.tar.gz, unknown), `restoreBackup` (psql / alpine tar)
- `RestoreCommand` (positional optional, --dir, --target)
- 5 tests MockAdapter
PRs : 22 — Tests : 69 — Phase 1 : 20/28
