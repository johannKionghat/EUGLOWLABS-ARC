# CLI-018 — `arc backup` ✅
PR #20 → `7012950`
- `runBackup(adapter, opts)` pg_dumpall + tar.gz volumes
- `BackupCommand` (--out-dir, --postgres, --volume repeatable)
- 3 tests MockAdapter
PRs : 20 — Tests : 61 — Phase 1 : 18/28
