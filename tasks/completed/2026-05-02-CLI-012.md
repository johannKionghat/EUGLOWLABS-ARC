# CLI-012 — `arc deploy` orchestrant adapter + Compose ✅

PR : #14 → `34692da` — Durée : ~50 min

## Bilan
- `deploy(cfg, adapter, opts)` ties config + adapter + templates + compose en une fonction pure orchestratrice
- Path posix pour cibles distantes uniformes
- VPS gated jusqu'à CLI-023
- 3 tests MockAdapter (dry-run, run, exit-code propagation)
- Bug fixé en route : `path.join` → `posix.join` pour les paths cibles
- Aucune décision ADR-level

## Stats
PRs : 14 — Tests : 45 arc-cli + 8 arc-shared = 53 — Phase 1 : 12/28
