# CLI-023 — `arc migrate` ✅
PR #25 → `049511e`
- `migrate(cfg, opts)` orchestrate backup→copyFile→deploy→restore
- `MigrateCommand` (--from, --to, --ssh-user, --ssh-key)
- 1 test dual-MockAdapter
PRs : 25 — Tests : 72 — Phase 1 : 23/28
