# Tâche terminée : CLI-005 — Commande `arc init` interactive

## Statut final
✅ Terminée — 2026-05-02
Durée réelle : ~1h45
PR : https://github.com/johannKionghat/EUGLOWLABS-ARC/pull/7 (mergée, squash, commit `fb5e10a`)

## Bilan
- **Ce qui s'est bien passé** : plan en 7 sous-tâches respecté. Découpage `prompts.ts` / `serialize.ts` / `write.ts` / `init.ts` propre et testable. Round-trip schéma → YAML → loader vérifié dans les tests. CI verte premier essai (Node 33s + Go 23s).
- **Ce qui a posé problème** :
  - Signature `validate` de clack 1.x : reçoit `string | undefined`, pas `string`. Les 4 validateurs corrigés (gardent guard `if (!value)`).
  - Le user a accidentellement reverté `tasks/INDEX.md` localement avant le merge — discard via `git checkout --` a remis l'état committé sur la branche feature.
- **Ce qu'on a appris** : tester les briques (`serialize`, `write`) en isolation suffit pour CLI-005 — mocker le stdin de `clack` est piégeux. Le pattern "command clipanion = orchestrator de fonctions pures + I/O minimal" est robuste, à reprendre pour CLI-012, CLI-015, etc.
- **Décisions prises** : aucune ADR-level. Toutes locales et réversibles.
  - Prompts limités aux champs essentiels (defaults zod sur stack/backups/services/projects)
  - Pas de E2E sur `init` — couverture par dogfood + briques en isolation
  - Flag `--out` ajouté pour faciliter tests E2E ultérieurs
  - Throw + catch côté Command plutôt que process.exit

## Dépassements de scope (mineurs)
- Validateurs adaptés à la signature clack `string | undefined` (corrige typecheck)

---

## Objectif (rappel)
Boucler l'écosystème de configuration : ajouter `arc init` qui pose les questions essentielles via `@clack/prompts`, valide à la volée, et écrit un `arc.config.yml` que `loadArcConfig` reparse sans erreur.

## Critères d'acceptation — tous validés
- [x] `arc init` affiche un intro clack propre puis enchaîne les prompts
- [x] Questions : project, target, domain, email, dns.zone, dns.api_token, et conditionnellement provider.{plan, location, ssh_key} si target=vps
- [x] Validation au moment de la saisie (slug, domain, email, non-empty)
- [x] Annulation Ctrl+C → exit 1 + "init cancelled"
- [x] Le YAML produit passe `loadArcConfig` (test write round-trip)
- [x] Refus d'écraser sans `--force`
- [x] 5 tests Vitest (2 serialize + 3 write — `InitCommand` non testé E2E par décision documentée)
- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [x] CI verte (Node 33s + Go 23s)
- [x] PR mergée sur main (#7 → fb5e10a)

## Smoke test reproduit en local
```
$ node packages/arc-cli/dist/index.js help
  arc help
  arc init [--out,-o #0] [--force,-f]
  arc version
```

## Fichiers livrés
- `packages/arc-cli/package.json` (modif — `@clack/prompts`)
- `packages/arc-cli/src/init/prompts.ts` (création — `promptForConfig()`)
- `packages/arc-cli/src/init/serialize.ts` (création — `serializeArcConfig()`)
- `packages/arc-cli/src/init/serialize.test.ts` (création — 2 tests)
- `packages/arc-cli/src/init/write.ts` (création — `writeArcConfig()` + garde-fou `--force`)
- `packages/arc-cli/src/init/write.test.ts` (création — 3 tests tmpdir)
- `packages/arc-cli/src/init/index.ts` (création — barrel)
- `packages/arc-cli/src/commands/init.ts` (création — `InitCommand`)
- `packages/arc-cli/src/cli.ts` (modif — register `InitCommand`)
- `packages/arc-cli/src/cli.test.ts` (modif — assertion `arc init` listé dans help)
- `pnpm-lock.yaml` (modif)
- `tasks/completed/2026-05-02-CLI-004.md` (archive CLI-004)

## Plan d'implémentation (rappel) — 7 sous-tâches toutes complétées
1. Dépendance @clack/prompts + bootstrap dossier ✅
2. serializeArcConfig + 2 tests ✅
3. writeArcConfig + 3 tests tmpdir ✅
4. promptForConfig (validateurs + branchement vps) ✅
5. InitCommand + flags --out/--force + wiring cli.ts ✅
6. Barrel + ajustement test help ✅
7. Vérif + commit + PR + merge ✅

## Statistiques projet à ce stade
- PRs mergées : 7
- Tests Vitest : arc-cli (17) + arc-shared (8) = **25**
- Phase 1 : 5/28 (CLI-001 → 005 ✅)
