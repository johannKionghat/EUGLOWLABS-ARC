# Tâche terminée : REFACTOR-002 — Refactor 🟧 + renommage HostAdapter (Phase D)

## Statut final
✅ Terminée — 2026-05-04
Durée réelle : ~2h (étalée sur la même session que REFACTOR-001)
Branche : `refactor/adr-0012-single-machine`
ADR : [ADR-0012](../../docs/03-architecture-decisions/0012-single-machine-install.md)

## Objectif
Adapter le code 🟧 au nouveau schéma single-machine, renommer `LocalAdapter` → `HostAdapter` (impl unique de `ExecutionAdapter`), réécrire le schéma config (`agent: { bind, port }` ajouté, `target` / `provider` / `tunnel` retirés), restaurer 100 % des tests cassés sans `it.skip`.

## Critères d'acceptation — TOUS validés
- [x] `LocalAdapter` renommée `HostAdapter` (`packages/arc-cli/src/exec/host.{ts,test.ts}`)
- [x] Tous les `import { LocalAdapter }` mis à jour (10 commandes + barrel `exec/index.ts`)
- [x] JSDoc nettoyé dans `exec/types.ts`, `exec/mock.ts`, `deploy/deploy.ts`, `ansible/run.ts`
- [x] Schéma `arc-shared/src/schemas/config.ts` : `target` / `provider` / `superRefine target↔provider` retirés ; bloc `agent: { bind: "127.0.0.1", port: 9999 }` ajouté
- [x] Fixture renommée `valid-local.yml` → `valid.yml`, débarassée du champ `target`
- [x] Fixture `invalid-schema.yml` adaptée (cas `target: azure` retiré)
- [x] `init/prompts.ts` : `select(target)`, branche vps, champ tunnel — supprimés
- [x] **78/78 tests verts** (arc-shared 7 + arc-cli 71). Aucun `it.skip` ajouté.
- [x] `pnpm install`, `pnpm test`, `pnpm typecheck`, `pnpm build`, `pnpm lint` — tous verts en run frais
- [x] Documentation : `docs/00-overview.md` réécrit, `docs/05-glossary.md` purgé, `CLAUDE.md` aligné, `README.md` quickstart mis à jour, `docs/03-architecture-decisions/0002-bun-runtime-cli.md` & `docs/04-conventions/testing.md` actualisés

## Commits
- `42a502b` — `refactor(cli): rename LocalAdapter to HostAdapter [REFACTOR-002]`
- `2fb06e5` — `test: rewire all tests to the post-refactor schema [REFACTOR-002]`
- `03bbdb8` — `docs: finalize Phase D — single-machine refactor doc cleanup [REFACTOR-002]`

## Bilan
- **Ce qui s'est bien passé** : restauration de tous les tests cassés sans skip. Tests `vitest` reportent 78/78 verts. Path Windows (`posix.join` → tests cross-platform) déjà géré en CLI-012, n'a pas eu à être retouché.
- **Ce qui a posé problème** : le 1er run audit avait des faux positifs à cause des `dist/` stales locaux (gitignored, mais sur disque). Nettoyés avant audit.
- **Ce qu'on a appris** : nettoyer les `dist/` locaux avant tout audit grep — sinon le bruit du build pré-refactor masque les vrais résidus.
- **Décisions prises** : aucune nouvelle (toutes actées Phase B).

## Scratchpad final
- 86 tests pré-refactor → 78 post-refactor = −8 tests, justifiés par fichier dans `docs/refactor-0012-completion.md`.
- `arc restore --target` conservé (sémantique = nom de volume Docker, sans rapport avec le dual local/vps) — faux positif sémantique légitime.

Suivante : REFACTOR-003.
