# Tâche terminée : CLI-007 — Génération template `docker-compose.sandbox.yml` avec isolation

## Statut final
✅ Terminée — 2026-05-02
Durée réelle : ~40 min
PR : https://github.com/johannKionghat/EUGLOWLABS-ARC/pull/9 (mergée, squash, commit `420797d`)

## Bilan
- **Ce qui s'est bien passé** : plan en 5 sous-tâches respecté à 100%, plus rapide que prévu (40 min au lieu de 55). Réutilisation directe du wrapper `renderTemplate()` de CLI-006 — pattern qui paye dès la 2ème utilisation. Tests verts du premier coup. CI verte premier essai (Node 32s + Go 24s).
- **Ce qui a posé problème** : rien.
- **Ce qu'on a appris** : la trilogie de templates (prod/sandbox/agents) est essentiellement du copy-paste structurel de spec-infra §19/§20 + un test de durcissement par template. Le découpage en files séparés `__templates__/*.eta` + `*-compose.ts` rend chaque ajout indépendant et trivial.
- **Décisions prises** : aucune ADR-level. Conformes à ADR-0008 (réseaux isolés).
  - `code-server` conservé (utile pour debug, mot de passe + accès via Traefik si besoin)
  - Pas de label Traefik sur la sandbox (pas d'exposition publique par défaut)
  - Pas de génération aléatoire du password (`__REPLACE_ME__` explicite)
  - Test E2E d'isolation effective reporté à AGENT-012 (besoin de Docker en CI)

---

## Objectif (rappel)
Frère symétrique de CLI-006. Ajouter le template eta de la sandbox isolée : `sandbox_net` avec `internal: true`, conteneur `code-executor` durci (`read_only`, `cap_drop: [ALL]`, `no-new-privileges`, mem/CPU limits), conteneur `code-server` pour debug. Implémentation directe d'**ADR-0008**.

## Critères d'acceptation — tous validés
- [x] Template eta `docker-compose.sandbox.yml.eta` versionné
- [x] `sandbox_net` avec `driver: bridge` ET `internal: true`
- [x] `code-executor` avec `read_only`, `cap_drop: [ALL]`, `no-new-privileges`, `mem_limit: 512m`, `cpus: 0.5`, `tmpfs`
- [x] `code-server` avec volume nommé `arc_sandbox_workspace`
- [x] `.env.eta` étendu avec `CODE_SERVER_PASSWORD=__REPLACE_ME__`
- [x] `generateSandboxCompose(cfg): string` exporté depuis `@euglowlabs/arc-cli/templates`
- [x] 4 tests Vitest
- [x] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` verts
- [x] CI verte (Node 32s + Go 24s)
- [x] PR mergée sur main (#9 → 420797d)

## Fichiers livrés
- `packages/arc-cli/src/templates/__templates__/docker-compose.sandbox.yml.eta` (création)
- `packages/arc-cli/src/templates/__templates__/env.eta` (modif — `CODE_SERVER_PASSWORD`)
- `packages/arc-cli/src/templates/sandbox-compose.ts` (création — `generateSandboxCompose`)
- `packages/arc-cli/src/templates/sandbox-compose.test.ts` (création — 4 tests)
- `packages/arc-cli/src/templates/index.ts` (modif — export du générateur)
- `packages/arc-cli/src/templates/env.test.ts` (modif — assertion `CODE_SERVER_PASSWORD`)
- `tasks/completed/2026-05-02-CLI-006.md` (archive CLI-006)

## Plan d'implémentation (rappel) — 5 sous-tâches toutes complétées
1. Template `docker-compose.sandbox.yml.eta` ✅
2. Étendre `env.eta` + ajustement test env ✅
3. Générateur `generateSandboxCompose` + barrel ✅
4. 4 tests Vitest ✅
5. Vérif + commit + PR + merge ✅

## Statistiques projet à ce stade
- PRs mergées : **9**
- Tests Vitest : arc-cli (27) + arc-shared (8) = **35**
- Phase 1 : 7/28 (CLI-001 → 007 ✅)
