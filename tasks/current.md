# Aucune tâche active

Pour démarrer une tâche, lance :

    /arc-task-start [TASK-ID]

Prochaine tâche suggérée : **ANSIBLE-001b** — rôles `coolify` + `ai-stack`.

Pré-requis recommandé avant 001b : créer `packages/arc-cli/playbooks/requirements.yml` pour pinner `community.general` (et probablement `community.docker`). Voir le bilan d'ANSIBLE-001a (`tasks/completed/2026-05-06-ANSIBLE-001a.md`) section « CLI gaps notés pour ANSIBLE-001b/c et futur ».
