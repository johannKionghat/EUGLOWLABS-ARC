---
# ARC setup playbook — STUB (INSTALL-002).
#
# This file is a no-op placeholder. The real installation logic
# (hardening UFW + fail2ban, Docker, Coolify, AI stack, sandbox,
# backups) is delivered by ANSIBLE-001.
#
# The stub exists so that `arc setup --apply` can exercise the full
# orchestration layer (detection, compose generation, ansible-playbook
# invocation) end-to-end, with the playbook returning success.
#
# Do NOT edit this file to add real tasks — write proper roles under
# packages/arc-cli/playbooks/roles/ as part of ANSIBLE-001.

- name: ARC setup stub
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - name: Notify stub status
      debug:
        msg: |
          ⏳ ARC setup playbook is currently a STUB (no-op).
          Real installation logic will be provided by ANSIBLE-001.
          This stub returns success to allow testing the orchestration layer.
          See tasks/INDEX.md for ANSIBLE-001 status.
