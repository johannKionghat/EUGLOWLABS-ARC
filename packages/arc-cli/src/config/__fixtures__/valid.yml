# Minimal valid arc.config.yml — defaults applied to agent/stack/backups/services.
project: johann-stack
domain: mondomaine.dev
email: johann@mondomaine.dev

dns:
  provider: cloudflare
  zone: mondomaine.dev
  api_token: cf-token-placeholder
