/**
 * Current version of the EuglowLabs ARC CLI.
 *
 * Kept manually in sync with `package.json#version` until CLI-025
 * introduces a build step that injects the version at compile time.
 */
export const VERSION = "0.0.0";
